apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "2"
  creationTimestamp: null
  generation: 1
  labels:
    app: kanboard
    version: stable
  name: kanboard
  selfLink: /apis/extensions/v1beta1/namespaces//deployments/kanboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kanboard
      version: stable
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: kanboard
        version: stable
      name: kanboard
    spec:
      containers:
      - image: kanboard/kanboard:stable
        imagePullPolicy: IfNotPresent
        name: kanboard
        resources: {}
        securityContext:
          privileged: false
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: {}
